import { UIColor, UIView, UIFont, attribute } from './UIView';
import { indent } from '@/utils';

export class UIPickerView extends UIView {
    name: string = "pickerView"
    className: string = "UIPickerView"


}